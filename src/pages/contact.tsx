import Nav from "@/components/Nav";
import React from "react";

const contact = ({ currentModal, setName }: any) => {
  return (
    <div>
      <Nav
        setName={setName}
        currentModal={currentModal}
    
      />
      contact
    </div>
  );
};

export default contact;
